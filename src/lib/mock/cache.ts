import type { CacheKey } from '@/lib/types';

/**
 * 12 Redis cache key patterns — identity lookups, consent, tariffs, TRA blocklist.
 * Hit rates and sizes reflect realistic UAE Telco operational scales.
 */
export const CACHE_KEYS: CacheKey[] = [
  {
    id: 'c-001',
    pattern: 'identity:dtx:*',
    description: 'DTX_ID → graph node lookup. Hot path for every identity-resolved pipeline.',
    ttlSeconds: 3600,
    hitRate: 0.78,
    sizeBytes: 4.2 * 1024 * 1024 * 1024,
    lastAccessed: 'just now',
    hotKeys: 1247,
  },
  {
    id: 'c-002',
    pattern: 'consent:purposes:*',
    description: 'Customer consent purpose set — read by every Privacy gate.',
    ttlSeconds: 600,
    hitRate: 0.91,
    sizeBytes: 480 * 1024 * 1024,
    lastAccessed: '2s ago',
    hotKeys: 892,
  },
  {
    id: 'c-003',
    pattern: 'tariff:plan:*',
    description: 'Plan code → tariff metadata for billing enrichment.',
    ttlSeconds: 86400,
    hitRate: 0.96,
    sizeBytes: 14 * 1024 * 1024,
    lastAccessed: '8s ago',
    hotKeys: 47,
  },
  {
    id: 'c-004',
    pattern: 'roaming:partner:*',
    description: 'Partner network → reconciliation rule version.',
    ttlSeconds: 1800,
    hitRate: 0.82,
    sizeBytes: 3.4 * 1024 * 1024,
    lastAccessed: '14s ago',
    hotKeys: 64,
  },
  {
    id: 'c-005',
    pattern: 'tra:blocklist:*',
    description: 'TRA-mandated MSISDN blocklist (regulatory). Refreshed every 5min.',
    ttlSeconds: 300,
    hitRate: 0.71,
    sizeBytes: 88 * 1024 * 1024,
    lastAccessed: 'just now',
    hotKeys: 38,
  },
  {
    id: 'c-006',
    pattern: 'cell:site:*',
    description: 'Cell site ID → geo + emirate + carrier zone.',
    ttlSeconds: 86400,
    hitRate: 0.99,
    sizeBytes: 2.1 * 1024 * 1024,
    lastAccessed: 'just now',
    hotKeys: 12,
  },
  {
    id: 'c-007',
    pattern: 'customer:segment:*',
    description: 'Customer segment classification (VIP, postpaid, prepaid, business).',
    ttlSeconds: 1800,
    hitRate: 0.86,
    sizeBytes: 920 * 1024 * 1024,
    lastAccessed: '4s ago',
    hotKeys: 412,
  },
  {
    id: 'c-008',
    pattern: 'fraud:risk:*',
    description: 'Real-time fraud risk score per MSISDN — feeds Privacy gate exit conditions.',
    ttlSeconds: 60,
    hitRate: 0.62,
    sizeBytes: 28 * 1024 * 1024,
    lastAccessed: 'just now',
    hotKeys: 156,
  },
  {
    id: 'c-009',
    pattern: 'schema:registry:*',
    description: 'Schema registry version cache — used by every DQ operator.',
    ttlSeconds: 600,
    hitRate: 0.94,
    sizeBytes: 6.8 * 1024 * 1024,
    lastAccessed: '11s ago',
    hotKeys: 24,
  },
  {
    id: 'c-010',
    pattern: 'iot:device:*',
    description: 'IoT device fingerprint → tenant + asset class.',
    ttlSeconds: 3600,
    hitRate: 0.74,
    sizeBytes: 1.8 * 1024 * 1024 * 1024,
    lastAccessed: 'just now',
    hotKeys: 5230,
  },
  {
    id: 'c-011',
    pattern: 'session:5g:*',
    description: '5G session anchor — consulted on every usage rollup.',
    ttlSeconds: 120,
    hitRate: 0.81,
    sizeBytes: 740 * 1024 * 1024,
    lastAccessed: 'just now',
    hotKeys: 1680,
  },
  {
    id: 'c-012',
    pattern: 'audit:tra:*',
    description: 'TRA audit lookup index — read-heavy, write-rare.',
    ttlSeconds: 3600,
    hitRate: 0.97,
    sizeBytes: 32 * 1024 * 1024,
    lastAccessed: '47s ago',
    hotKeys: 18,
  },
];

export function getCacheKey(id: string): CacheKey | undefined {
  return CACHE_KEYS.find((k) => k.id === id);
}
